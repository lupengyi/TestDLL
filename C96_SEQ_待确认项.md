# C96 主驱与辅驱 SEQ 待确认项

当前工程草案：`TestDLL\bin\Sequence\C96_FCT_主驱辅驱_A0_工程草案.json`

## 已按现有代码和 Locator 落地

- C96 进入 FT、通信初始化和已验证的故障清除。
- TM1：Motor Control `0x68`、Motor Status `0x6C`、Current Result `0x7C`。
- TM2：Motor Control `0x80`、Motor Status `0x84`、Current Result `0x94`。
- TM1/TM2 电流点：0、100、200、300、400、500、600、700、850 Arms。
- 每个电流点均包含写入、稳定等待、故障读取、三相 RMS 读取。
- 辅驱 DBC 反馈信号名称已经与当前 DBC 文件核对。
- 所有未确认的输出动作默认 `RunMode=Skip`，避免直接动作硬件。

## 需要用户或客户确认

1. 两块 `SHT_48SEDO_A` 的最终 IP、端口和从站地址。当前使用可编辑占位 IP：`192.168.0.124`、`192.168.0.125`。
2. C96 双旋变是否使用两台旋变模拟器；当前只有一个 `RESOLVERCAN` 通道配置。
3. 主驱各电流点的正式 LIMIT。草案暂按目标值 ±10%，仅作为工程占位。
4. PDU `0x0CF2503B` 的最终 DATA，以及主驱、辅驱、预充继电器的闭合/断开顺序；周期已确定为100ms。
5. 油泵参数已按17A、50Hz、VF 1.9%写入，仍需确认运行时间。
6. 气泵参数已按17A、50Hz、VF 1.9%写入，仍需确认运行时间及气泵控制帧信号归属。
7. DCDC电子负载电流已设为100A；仍需确认目标输出电压、稳定时间、正式LIMIT及AN23600E量程能力。
8. 主驱 618V/450V/750V 与 630A/850A 温升矩阵是否全部执行，还是按产品功率型号选择子集。
9. 冷却水流量、水压和温度由 PLC/DAQ 哪些地址读取；测规给出要求，但当前缺少确定的仪器地址映射。
10. 被动/主动放电的产品信号映射、继电器通道以及是否允许调试工具自动执行高压步骤。

## IO编号规则

- 表格 `OUT1` 对应软件底层通道 `0`，`OUT48` 对应通道 `47`。
- 调试工具允许直接输入 `OUT1,OUT5`，MainTest自动转换成底层通道 `0,4`。
- 高压测量板15选1已增加专用动作：先拉高OUT5/OUT6，再按通道1~15设置OUT1~OUT4，等待后拉低OUT5。
