# FCT机台部署说明

## 推荐方式

运行仓库根目录的 `build-machine-package.ps1`，生成：

- `artifacts\FCT-Machine-x86\DebugTool`：完整独立Debug工具。
- `artifacts\FCT-Machine-x86\PlatformPatch`：原FCT平台补丁。
- `artifacts\FCT-Machine-x86.zip`：可直接复制到机台电脑。

## 独立Debug工具

将 `DebugTool` 整个文件夹复制到机台电脑，不能只复制EXE。启动 `ManualCanDebug.exe`。

通信配置位于 `DebugTool\Config\InstrumentConfig.json`，也可以在软件的“高级工具 → 仪器中心”中配置。

## 原FCT平台

1. 关闭原FCT平台并备份其DLL和Config目录。
2. 将 `PlatformPatch\DLLs` 内全部文件复制到平台实际DLL目录，覆盖旧的 `CSP.TestDLL.dll`，并保留 `kerneldlls` 子目录。
3. 将 `PlatformPatch\Config` 内配置复制到平台实际Config目录。现场已有产品配置时先比较后合并；至少必须更新 `InstrumentConfig.json`、`InstrumentCatalog.json`、`InstrumentActions.json` 和使用到的DBC。
4. 根据现场资源填写 `InstrumentConfig.json`，逐台将 `Initialize` 改为 `true` 做初始化验证。

## 闭环关系

- Debug工具把仪器中心勾选结果传给 `CSP.TestDLL.dll` 的 `FCT_SetInstrumentSelection`。
- 原平台没有该调用时，MainTest会从程序旁的 `Config\InstrumentConfig.json` 读取 `Initialize=true` 的仪器。
- SEQ中的 `Device` 经过 `FCT_ExecuteAction` 分发到对应真实仪器实例。
- 停止、完成或异常时执行统一安全收尾：关闭电源输出、电子负载和继电器输出，并停止CAN周期发送。

程序为.NET Framework 4.8、x86。机台电脑必须使用32位进程，并安装对应仪器厂商驱动/VISA运行时。
